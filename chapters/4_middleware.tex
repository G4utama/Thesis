\chapter{Middleware}
\label{Middleware}

\section{Introduzione al concetto di Middleware}
Nell'ambito dei sistemi informativi moderni, la crescente necessità di integrare applicazioni eterogenee, spesso sviluppate con tecnologie diverse e distribuite su più nodi, ha reso centrale il ruolo del middleware. Con questo termine si indica genericamente quello strato software che si colloca tra il sistema operativo e le applicazioni, con lo scopo di semplificare la comunicazione, la gestione dei dati e l'interoperabilità tra componenti distinte di un sistema distribuito. \\ \\
Il presente tirocinio si colloca proprio in questo ambito: l'obiettivo è studiare, progettare e implementare un middleware in grado di leggere dati da un topic Kafka, arricchirli con informazioni provenienti da un database relazionale \nameref{PostgreSQL}, e pubblicare il risultato su un secondo topic Kafka. Questo capitolo introduce i concetti teorici alla base del middleware in generale, per poi restringere il campo alle tecnologie specifiche impiegate nel progetto: \nameref{Spring} Boot, \nameref{Spring} Data JPA e \nameref{Apache Kafka}.

\section{Definizione e ruolo del Middleware}
Il middleware è un tipo di programma software che fornisce alle applicazioni software servizi che vanno oltre quelli offerti dal sistema operativo.
È lo strato intermedio tra le applicazioni e le componenti sottostanti, come sistemi operativi o database, facilitando cpsì la comunicazione, l'integrazione e la gestione delle risorse nei sistemi distribuiti. 
Il suo ruolo primario è quello di nascondere la complessità dell'infrastruttura sottostante, garantendo interoperabilità, scalabilità e sicurezza. \\ \\
Le caratteristiche principali sono:
\begin{itemize}
    \item \textbf{Disaccoppiamento:} i sistemi produttori e consumatori di informazioni non devono obbligatoriamente conoscersi o condividere lo stesso linguaggio, piattaforma o formato dati.
    \item \textbf{Interoperabilità:} consente la comunicazione tra applicazioni scritte in linguaggi diversi o eseguite su piattaforme differenti.
    \item \textbf{Scalabilità e affidabilità:} molti middleware offrono tolleranza ai guasti e garanzie di consegna dei messaggi.
\end{itemize}
Il middleware sviluppato in questo progetto ha la funzione di ponte tra due flussi di eventi Kafka, arricchendo il dato in transito con informazioni recuperate da \nameref{PostgreSQL}. 
Si tratta quindi di un middleware orientato ai dati e agli eventi, piuttosto che di un semplice bus di comunicazione \acrshort{RPC}.

\section{Classificazione dei Middleware}
Sono state definite numerose categorie di middleware, in base al settore in cui vengono impiegate o al modulo applicativo che servono. 
Una bibliografia recente ha identificato le principali categorie di middleware come segue: 
\begin{itemize}
    \item \textbf{Transactional Middleware:} elaborazione di transazioni multiple sincrone/asincrone, fungendo da cluster di richieste associate provenienti da sistemi distribuiti (es. transazioni bancarie o pagamenti con carta di credito).
    \item \textbf{Message-Oriented Middleware (\acrshort{MOM}):} basati sullo scambio di messaggi asincroni attraverso code o topic (es. \nameref{Apache Kafka}).
    \item \textbf{Procedural Middleware:} architetture remote e locali per connettersi, trasmettere e recuperare le risposte software delle comunicazioni tra sistemi asincroni (operazione di chiamata).
    \item \textbf{Object-Oriented Middleware:} simile al middleware procedurale, ma incorpora i principi di progettazione della programmazione orientata agli oggetti. Dal punto di vista analitico, i suoi componenti software comprendono riferimenti agli oggetti, eccezioni ed ereditarietà delle proprietà tramite richieste di oggetti distribuiti.
\end{itemize}
Il progetto oggetto del tirocinio si colloca nella seconda categoria: si tratta infatti di un Message-Oriented Middleware con funzionalità di arricchimento dati (data enrichment), in cui il canale di comunicazione è rappresentato da Kafka e la logica di trasformazione è delegata a un componente applicativo (\nameref{Spring}).

\section{Enterprise Integration Patterns}
\label{Enterprise Integration Patterns}
Per inquadrare correttamente il problema dal punto di vista architetturale, è utile fare riferimento agli Enterprise Integration Patterns (\acrshort{EIP}), un catalogo di pattern ricorrenti nell'integrazione di sistemi enterprise, che permette di descrivere il problema del progetto in termini standard e riconosciuti in letteratura.

\subsection{Content Enricher}
\label{Content Enricher}
Il pattern più rilevante per questo lavoro è il Content Enricher: un componente che riceve un messaggio contenente informazioni parziali e lo arricchisce con dati aggiuntivi provenienti da una fonte esterna (database relazionale), producendo un messaggio più completo da inoltrare al passo successivo della pipeline.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{assets/contentEnricher.drawio.png}
    \caption{Content Enricher}
\end{figure}
\noindent
Nell'implementazione sviluppata, questo pattern è realizzato concretamente dalla classe \textcolor{purple}{EnrichmentService}, che riceve in input un \textcolor{purple}{OrderEvent} (contenente, tra gli altri campi, un \textcolor{purple}{customerId}), interroga \nameref{PostgreSQL} tramite \textcolor{purple}{CustomerRepository} (Spring Data JPA) per recuperare i dati del cliente corrispondente, e produce in output un \textcolor{purple}{EnrichedOrder}, ovvero il messaggio arricchito con le informazioni mancanti.

\subsection{Dead Letter Channel}
\label{Dead Letter Channel}
Un secondo pattern, altrettanto rilevante, è il Dead Letter Channel. Questo pattern affronta un problema tipico dei sistemi di integrazione basati su messaggi: cosa fare quando un messaggio non può essere elaborato correttamente? Le opzioni alternative sono entrambe insoddisfacenti:
\begin{itemize}
    \item Bloccare la pipeline, in attesa che il problema venga risolto manualmente, con il rischio di fermare l'elaborazione di tutti i messaggi successivi (in Kafka, il blocco su una partizione impedisce l'avanzamento dell'offset per l'intera partizione);
    \item Scartare silenziosamente il messaggio, con conseguente perdita di dati e nessuna possibilità di analisi o recupero successivo.
\end{itemize}
Il pattern Dead Letter Channel propone invece una terza via: i messaggi che non possono essere elaborati vengono instradati su un canale separato (la dead letter queue, o \acrshort{DLQ}), dedicato esclusivamente alla raccolta dei messaggi "falliti". 
Questo consente al flusso principale di proseguire senza interruzioni, mantenendo al contempo traccia dei messaggi problematici per un'analisi o un riprocessamento successivo.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{assets/deadLetterChannel.drawio.png}
    \caption{Content Enricher}
\end{figure}
\noindent
Nel progetto, questo pattern è implementato nel modo seguente: quando \textcolor{purple}{EnrichmentService} tenta di arricchire un \textcolor{purple}{OrderEvent} con un \textcolor{purple}{customerId} non presente su PostgreSQL, viene sollevata una \textcolor{purple}{CustomerNotFoundException}. 
Questa eccezione viene intercettata e gestita instradando il messaggio originale sul topic \textcolor{purple}{orders-input-dlq}, anziché proseguire verso \textcolor{purple}{EnrichedProducer} e il topic \textcolor{purple}{orders-output}. 
In questo modo, un singolo dato incompleto o malformato non compromette l'elaborazione degli ordini successivi, e resta comunque disponibile per un'ispezione a posteriori (ad esempio tramite la Kafka UI predisposta nell'ambiente di sviluppo).

\subsection{Altri pattern rilevanti}
\label{Message Channel}
\label{Point-to-Point Channel}
\label{Message Translator}
Altri pattern \acrshort{EIP} utili e applicabili al progetto sono:
\begin{itemize}
    \item \textbf{Message Channel:} collega le applicazioni tramite un canale di messaggi, in cui un’applicazione scrive le informazioni nel canale e l’altra le legge dal canale. I messaggi qui rappresentato dai topic Kafka (\textcolor{purple}{orders-input}, \textcolor{purple}{orders-output}, \textcolor{purple}{orders-input-dlq}).
    \begin{figure}[H]
        \centering
        \includegraphics[width=0.75\textwidth]{assets/messageChannel.drawio.png}
        \caption{Message Channel}
    \end{figure}
    \item \textbf{Point-to-Point Channel:} invia il messaggio su un canale Point-to-Point, che garantisce che solo un destinatario riceva un determinato messaggio.
    \begin{figure}[H]
        \centering
        \includegraphics[width=0.75\textwidth]{assets/pointToPointChannel.drawio.png}
        \caption{Point-to-Point Channel}
    \end{figure}
    \item \textbf{Message Translator:} utilizza un filtro speciale (Translator), tra altri filtri o applicazioni per convertire un formato di dati in un altro. Nel progetto è usato nella conversione tra \textcolor{purple}{OrderEvent} e \textcolor{purple}{EnrichedOrder}, entrambi \acrshort{DTO} distinti nel package \textcolor{purple}{dto/}.
    \begin{figure}[H]
        \centering
        \includegraphics[width=0.5\textwidth]{assets/messageTranslator.drawio.png}
        \caption{Message Translator}
    \end{figure}
\end{itemize}

\section{Apache Kafka come servizio di messaggistica}
\label{Apache Kafka come servizio di messaggistica}

\subsection{Introduzione e caratteristiche generali}
\nameref{Apache Kafka} è una piattaforma distribuita per lo streaming di eventi, originariamente sviluppata da LinkedIn\footnote{LinkedIn - URL: \url{https://www.linkedin.com}} e successivamente rilasciata come progetto open source sviluppata da Apache Software Foundation\footnote{Apache Software Foundation - URL: \url{https://www.apache.org/}}. \\ \\
A differenza dei tradizionali sistemi di messaggistica basati su code (come i classici Message Queue), Kafka nasce con l'obiettivo di gestire flussi di dati ad alto \textit{throughput}, bassa latenza e con garanzie di persistenza e ripetibilità della lettura, caratteristiche che lo rendono particolarmente adatto sia a scenari di semplice trasporto di messaggi, sia a scenari più complessi di \textit{event streaming} ed \textit{event sourcing}. \\ \\
Nel contesto di questo progetto, Kafka non è impiegato semplicemente come canale di trasporto, ma come componente architetturale a tutti gli effetti: da esso dipendono sia l'ingresso dei dati grezzi nel sistema, sia l'uscita dei dati arricchiti.

\subsection{Event streaming}
L'\textit{event streaming} è l'equivalente digitale del sistema nervoso centrale del corpo umano. Rappresenta il fondamento tecnologico di un mondo “sempre attivo”, in cui le aziende sono sempre più definite dal \textit{software} e automatizzate, e in cui l'utente del \textit{software} è a sua volta un \textit{software}. \\ \\
Dal punto di vista tecnico, lo streaming di eventi consiste in:
\begin{itemize}
    \item acquisizione in tempo reale di dati provenienti da fonti quali \textit{database}, sensori, dispositivi mobili, servizi cloud e applicazioni software sotto forma di flussi di eventi; 
    \item archiviazione duratura di tali flussi per un successivo recupero; nella manipolazione, elaborazione e risposta ai flussi di eventi sia in tempo reale che a posteriori; 
    \item instradamento dei flussi di eventi verso diverse tecnologie di destinazione a seconda delle necessità.
\end{itemize}
L'\textit{event streaming} garantisce quindi un flusso continuo e l'interpretazione dei dati, in modo che le informazioni giuste si trovino nel posto giusto, al momento giusto.

% \subsection{Use cases}
% Lo streaming di eventi trova applicazione in un’ampia varietà di casi d’uso in una miriade di settori e organizzazioni. Tra i numerosi esempi figurano:
% \begin{itemize}
%     \item L’elaborazione in tempo reale di pagamenti e transazioni finanziarie, ad esempio nelle borse valori, nelle banche e nelle compagnie assicurative.
%     \item Il tracciamento e il monitoraggio in tempo reale di auto, camion, flotte e spedizioni, ad esempio nel settore della logistica e in quello automobilistico.
%     \item Per acquisire e analizzare continuamente i dati dei sensori provenienti da dispositivi IoT o altre apparecchiature, ad esempio nelle fabbriche e nei parchi eolici.
%     \item Per raccogliere le interazioni e gli ordini dei clienti e reagire immediatamente, ad esempio nel settore della vendita al dettaglio, in quello alberghiero e dei viaggi e nelle applicazioni mobili.
%     \item Per monitorare i pazienti in ambito ospedaliero e prevedere i cambiamenti delle loro condizioni, al fine di garantire un trattamento tempestivo in caso di emergenza.
%     \item Per collegare, archiviare e rendere disponibili i dati prodotti dalle diverse divisioni di un’azienda.
%     \item Fungere da base per piattaforme dati, architetture basate sugli eventi e microservizi.
% \end{itemize}

\subsection{Architettura e concetti fondamentali}
\label{Architettura e concetti fondamentali}
\paragraph{Producer e Consumer} I produttori sono quelle applicazioni client che pubblicano (scrivono) eventi su Kafka, mentre i consumatori sono quelle che si abbonano (leggono ed elaborano) a tali eventi. 
In Kafka, produttori e consumatori sono completamente disaccoppiati e indipendenti l'uno dall'altro, il che costituisce un elemento chiave del design per ottenere l'elevata scalabilità (i produttori non devono mai attendere i consumatori). \\ \\
Nel middleware sviluppato, l'applicazione stessa riveste entrambi i ruoli: agisce come consumer rispetto al topic sorgente (da cui legge i dati grezzi da arricchire) e come producer rispetto al topic di destinazione (su cui scrive il dato arricchito).
\paragraph{Consumer Group} Kafka introduce il concetto di consumer group, un insieme di consumer identificati da uno stesso \textcolor{purple}{group.id}, che si ripartiscono automaticamente le partizioni di un topic, così che ciascun messaggio venga elaborato da un solo consumer all'interno del gruppo. Questo meccanismo consente di scalare orizzontalmente il middleware, aggiungendo istanze aggiuntive dell'applicazione senza necessità di logica di bilanciamento del carico esplicita: sarà Kafka stesso a ridistribuire le partizioni tra i consumer disponibili (fenomeno noto come rebalance).
\paragraph{Event} Gli eventi vengono organizzati e archiviati in modo permanente in argomenti (topic). Un argomento è simile a una cartella in un file system, mentre gli eventi sono i file contenuti in quella cartella. \\ \\
Gli argomenti in Kafka sono sempre multi-producer e multi-consumer : un argomento può avere zero, uno o più produttori che vi scrivono eventi, così come zero, uno o più consumatori che si abbonano a tali eventi. Gli eventi in un argomento possono essere letti tutte le volte che è necessario: a differenza dei sistemi di messaggistica tradizionali, gli eventi non vengono eliminati dopo la lettura. È l'utente a definire per quanto tempo Kafka debba conservare gli eventi tramite un'impostazione di configurazione specifica per ogni argomento, trascorso il quale gli eventi più vecchi verranno eliminati. Le prestazioni di Kafka rimangono sostanzialmente costanti indipendentemente dalla dimensione dei dati, quindi l'archiviazione dei dati per lunghi periodi di tempo non presenta alcun problema.
\paragraph{Topic} L'unità logica fondamentale di Kafka è il topic, un canale a cui i producer inviano messaggi e da cui i consumer li leggono. \\ \\
Ogni topic è fisicamente suddiviso in una o più partition, che rappresentano l'unità di parallelismo: ogni partizione è una sequenza ordinata e immutabile di messaggi, identificati da un numero progressivo detto offset.
Questa distribuzione dei dati è molto importante per la scalabilità, poiché consente alle applicazioni client di leggere e scrivere i dati da e verso molti broker contemporaneamente. Quando un nuovo evento viene pubblicato su un argomento, viene effettivamente aggiunto a una delle partizioni dell'argomento. \\ \\
Per garantire la tolleranza ai guasti e l'alta disponibilità dei dati, ogni argomento può essere replicato, anche tra diverse aree geografiche o data center, in modo che ci siano sempre più broker in possesso di una copia dei dati nel caso in cui si verifichino problemi, si desideri eseguire operazioni di manutenzione sui broker e così via. 
Un'impostazione comune in produzione è un fattore di replica pari a 3, ovvero ci saranno sempre tre copie dei dati. Questa replica viene eseguita a livello di partizioni degli argomenti.

\paragraph{Broker e Cluster} Un cluster Kafka è composto da uno o più broker, ciascuno responsabile della gestione di un sottoinsieme di partizioni. La distribuzione dei dati su più broker, unita alla replica delle partizioni (controllata dal parametro replication factor), garantisce tolleranza ai guasti: in caso di indisponibilità di un broker, un'altra replica può assumere il ruolo di leader per la partizione interessata, senza perdita di dati.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{assets/apacheKafka.drawio.png}
    \caption{Apache Kafka}
\end{figure}
% Esempio di configurazione Kafka:
% \begin{minted}{yaml}
% spring:
%   kafka:
%     bootstrap-servers: localhost:9092

%     consumer:
%       group-id: middleware-enrichment-group
%       auto-offset-reset: earliest
%       enable-auto-commit: false # commit manuale dell'offset -> semantica at-least-once
%       key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
%       value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
%       properties:
%         spring.json.trusted.packages: "*"

%     producer:
%       key-serializer: org.apache.kafka.common.serialization.StringSerializer
%       value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
%       acks: all # garantisce che il messaggio sia replicato prima dell'ack

%     template:
%       default-topic: topic-dati-arricchiti

%   datasource:
%     url: jdbc:postgresql://localhost:5432/enrichment_db
%     username: middleware_user
%     password: ${DB_PASSWORD}
%   jpa:
%     hibernate:
%       ddl-auto: validate
% \end{minted}

\subsection{Persistenza dei messaggi}
Una delle caratteristiche distintive di Kafka rispetto ai tradizionali MOM è la persistenza dei messaggi su disco, per un periodo di retention configurabile, indipendentemente dal fatto che siano già stati letti da un consumer. \\ \\
Questo consente di:
\begin{itemize}
    \item rileggere lo storico dei messaggi in caso di errore o necessità di riprocessamento;
    \item gestire il disaccoppiamento temporale tra producer e consumer, che non devono essere necessariamente attivi nello stesso momento;
    \item garantire resilienza del middleware in caso di malfunzionamento temporaneo della componente di arricchimento (indisponibilità del database), poiché i messaggi restano disponibili nel topic finché non vengono correttamente processati.
\end{itemize}
\subsection{Semantica di consegna}
Dal punto di vista della semantica di consegna, Kafka supporta tre modalità:
\begin{itemize}
    \item \textbf{At-most-once:} il messaggio può essere perso, ma non elaborato più volte (offset committato prima dell'elaborazione).
    \item \textbf{At-least-once:} il messaggio viene sempre elaborato, ma può essere elaborato più volte in caso di errore o riavvio (offset committato dopo l'elaborazione). È la modalità più comune nei sistemi che, come quello del progetto, non tollerano perdita di dati.
    \item \textbf{Exactly-once:} garantisce che ogni messaggio venga elaborato esattamente una volta, tramite meccanismi di transazioni Kafka.
\end{itemize}
Nel contesto del middleware sviluppato, la scelta della semantica di consegna è un aspetto progettuale rilevante: poiché la fase di arricchimento comporta un'interrogazione al database PostgreSQL, occorre valutare attentamente le implicazioni di un'eventuale elaborazione duplicata del medesimo messaggio.

% \subsection{Ruolo di Kafka nell'architettura del middleware}
% Nel sistema oggetto del tirocinio, Kafka assume un triplice ruolo architetturale, come illustrato nello schema architetturale (\ref{fig:Middleware}):
% \begin{itemize}
%     \item \textbf{Sorgente dei dati grezzi:} il \textcolor{purple}{topic orders-input} riceve eventi ordine in formato \textcolor{purple}{OrderEvent}, contenenti tra gli altri campi un identificativo (\textcolor{purple}{customerId}) che fa riferimento a un'entità anagrafica non ancora risolta. Il middleware si comporta da consumer rispetto a questo topic tramite la classe \textcolor{purple}{OrderConsumer}, annotata con \textcolor{purple}{@KafkaListener}.
%     \item \textbf{Destinazione del dato arricchito:} una volta recuperate le informazioni del cliente da \nameref{PostgreSQL} tramite \textcolor{purple}{EnrichmentService} e composto il messaggio finale (\textcolor{purple}{EnrichedOrder}), il middleware agisce da producer, tramite la classe \textcolor{purple}{EnrichedProducer}, verso il topic \textcolor{purple}{orders-output}, reso disponibile ai sistemi a valle.
%     \item \textbf{Canale per la gestione degli errori:} qualora il processo di arricchimento fallisca (ad esempio per un \textcolor{purple}{customerId} non presente su \nameref{PostgreSQL}, con conseguente sollevamento di una \textcolor{purple}{CustomerNotFoundException}), il messaggio originale viene instradato su un terzo topic, \textcolor{purple}{orders-input-dlq}, anziché su \textcolor{purple}{orders-output}. Questo terzo ruolo realizza concretamente il pattern Dead Letter Channel (\ref{Dead Letter Channel}), e rappresenta un elemento di robustezza rilevante ai fini della continuità del flusso di elaborazione.
% \end{itemize}
% Ciascuno di questi tre topic è configurato con un numero di partizioni e un fattore di replica specifici: \textcolor{purple}{orders-input} e \textcolor{purple}{orders-output} sono creati con 3 partizioni (per consentire il parallelismo in lettura/scrittura), mentre \textcolor{purple}{orders-input-dlq} è configurato con una singola partizione, essendo un canale a più basso volume atteso e per il quale non è prioritario il parallelismo, quanto piuttosto la semplicità di ispezione dei messaggi falliti (ad esempio tramite la Kafka UI predisposta nell'ambiente di sviluppo, accessibile all'indirizzo \textcolor{purple}{http://localhost:8081}). \\ \\
% Questa impostazione architetturale, oltre a realizzare concretamente il pattern Content Enricher (\ref{Content Enricher}), consente di ottenere un sistema fortemente disaccoppiato e resiliente: né la sorgente originaria degli ordini, né i sistemi consumatori del dato finale devono avere conoscenza diretta dell'esistenza del middleware, della logica di arricchimento o della sua eventuale gestione degli errori, essendo l'unico punto di contatto rappresentato dai topic Kafka stessi. In particolare, il fatto che gli errori vengano isolati su un canale dedicato, anziché propagarsi al topic di output o bloccare la pipeline, costituisce un requisito di robustezza spesso implicito ma non scontato in un middleware di enrichment in produzione.
% \begin{figure}[H]
%     \centering
%     \includegraphics[width=0.75\textwidth]{assets/middleware.drawio.png}
%     \caption{Middleware}
%     \label{fig:Middleware}
% \end{figure}

\section{Spring come piattaforma middleware}
\label{Spring Framework}
% La prima implementazione del middleware, oggetto principale del tirocinio, è stata realizzata utilizzando l'ecosistema Spring, largamente adottato in ambito enterprise per la costruzione di applicazioni Java robuste e modulari. \\ \\
% \textbf{Spring Boot} costituisce il framework di base, offrendo auto-configurazione, gestione semplificata delle dipendenze e un modello a container leggero (embedded server), che riduce drasticamente il tempo necessario per portare in produzione un servizio. \\ \\
% \textbf{Spring Data JPA} viene impiegato per la gestione dell'accesso al database PostgreSQL, secondo cui i dati necessari all'arricchimento del messaggio Kafka sono modellati tramite entità JPA e recuperati tramite repository, astraendo la scrittura di query SQL esplicite. \\ \\
% \textbf{Spring Kafka} viene utilizzato per orchestrare il flusso di elaborazione: lettura del messaggio dal topic sorgente, invocazione della logica di arricchimento tramite interrogazione al database, e scrittura del messaggio arricchito sul topic di destinazione.
Spring è tra i framework più diffusi in ambito enterprise per la costruzione di applicazioni Java robuste, modulari e facilmente integrabili con infrastrutture di messaggistica e basi di dati.

\subsection{Spring Boot}
\label{Spring Boot}
Spring Boot rappresenta l'evoluzione del framework Spring pensata per semplificare la configurazione e l'avvio di applicazioni Java, tramite il paradigma dell'auto-configuration e della convention over configuration. \\ \\
Alla base del framework resta il principio di \textit{Inversion of Control} (IoC), realizzato tramite \textit{Dependency Injection}: i componenti applicativi non istanziano direttamente le proprie dipendenze, ma le ricevono da un contenitore (l'application context), che ne gestisce il ciclo di vita. \\ \\
Ai fini della realizzazione di un middleware, questo approccio offre alcuni vantaggi rilevanti:
\begin{itemize}
    \item disaccoppiamento tra i vari componenti logici del sistema (lettura dei messaggi, logica di arricchimento, scrittura dei messaggi), che possono essere sviluppati, testati e sostituiti in modo indipendente;
    \item integrazione facile e veloce con le principali infrastrutture \textit{enterprise} (\textit{broker} di messaggistica, basi di dati, sistemi di monitoraggio), grazie a un ricco ecosistema di starter e librerie ufficiali;
    \item riduzione del codice infrastrutturale a carico dello sviluppatore, che può concentrarsi sulla logica di dominio.
\end{itemize}
\subsubsection{Auto-configuration}
La configurazione automatica di Spring Boot è una funzionalità che configura automaticamente i componenti dell'applicazione in base alle dipendenze disponibili. \\ \\
Crea e configura automaticamente i servizi e le impostazioni necessari per l'applicazione.
Riduce la configurazione manuale e accelera lo sviluppo. \\ \\
La configurazione automatica funziona tramite l'annotazione \textcolor{purple}{@EnableAutoConfiguration}.
\subsubsection{Convention over configuration}
Il principio \textit{Convention over configuration} (noto anche come \textit{coding by convention}) è un paradigma di progettazione software utilizzato dai framework che mira a ridurre il numero di decisioni che uno sviluppatore che utilizza il framework è tenuto a prendere, senza necessariamente rinunciare alla flessibilità e ai principi del \textit{Don't Repeat Yourself} (DRY). \\ \\
La frase significa essenzialmente che uno sviluppatore deve specificare solo gli aspetti non convenzionali dell'applicazione.
Quando la convenzione implementata dallo strumento corrisponde al comportamento desiderato, il sistema si comporta come previsto senza dover scrivere file di configurazione. Solo quando il comportamento desiderato si discosta dalla convenzione implementata è necessaria una configurazione esplicita.

\subsection{Spring Data JPA}
\label{Spring Data JPA}
Spring Data JPA è il modulo dell'ecosistema Spring dedicato alla persistenza dei dati, costruito sopra la specifica JPA (Java Persistence API) e sull'implementazione di riferimento Hibernate. \\ \\
Il suo contributo principale, dal punto di vista architetturale, è l'astrazione dell'accesso ai dati tramite il pattern Repository.
Lo sviluppatore definisce un'interfaccia che descrive le operazioni di accesso desiderate, senza doverne scrivere l'implementazione e le query SQL, che vengono generate automaticamente dal framework a partire dalle convenzioni di naming o da annotazioni dedicate. \\ \\
Nel contesto di un middleware di enrichment, questo modulo assume un ruolo centrale: è il componente che permette di interrogare la fonte dati esterna (PostgreSQL) necessaria a completare le informazioni presenti nel messaggio in transito, disaccoppiando la logica di arricchimento dal dettaglio implementativo dell'accesso al database.

\subsection{Spring Kafka}
\label{Spring Kafka}
Per la parte di integrazione con Apache Kafka, l'ecosistema Spring mette a disposizione Spring Kafka, un modulo che fornisce un'astrazione ad alto livello sopra il client Kafka nativo, semplificando la configurazione dei producer e dei consumer e integrandoli pienamente con il modello di \textit{dependency injection} tipico di Spring Boot. \\ \\
Il concetto centrale su cui si fonda Spring Kafka è quello di \textit{listener}: un componente applicativo si registra come consumatore di uno o più topic e viene invocato automaticamente ogni volta che un nuovo messaggio è disponibile, senza che lo sviluppatore lo debba gestire. Il modello di elaborazione risultante è quindi \textit{event-driven} e continuo: l'applicazione resta costantemente in ascolto, reagendo agli eventi non appena questi si presentano, senza necessità di una schedulazione periodica. \\ \\
Questo paradigma si adatta molto bene a scenari come quello affrontato nel presente lavoro, in cui il flusso di dati non ha una fine definita a priori (uno stream potenzialmente illimitato di eventi) e in cui è desiderabile minimizzare la latenza tra la produzione di un evento e la sua elaborazione. Spring Kafka realizza , a livello applicativo, le astrazioni Kafka già introdotte nella sezione \ref{Architettura e concetti fondamentali} (topic, consumer group, offset). \\ \\
Questi elementi rendono Spring Kafka una scelta architetturale coerente con il pattern Content Enricher (descritto in \ref{Content Enricher}): il \textit{listener} funge da punto di ingresso del messaggio grezzo, delega la logica di arricchimento a un componente dedicato (tipicamente interfacciato con la base dati tramite Spring Data JPA), e infine si occupa della pubblicazione del messaggio arricchito sul topic di destinazione, chiudendo così il ciclo di elaborazione descritto nello schema architetturale generale del middleware.

% \begin{figure}[H]
%     \centering
%     \includegraphics[width=0.4\textwidth]{assets/app.drawio.png}
%     \caption{Topic Kafka}
% \end{figure}

% \section{Apache Flink come alternativo paradigma}
% Differenza concettuale chiave: Flink è un motore di stream processing nativo (elaborazione continua, stateful), mentre Spring Boot/Batch è più orientato a job schedulati o microservizi request/event-driven
% Concetti Flink rilevanti: DataStream API, operatori di enrichment (es. join con stato esterno, async I/O verso PostgreSQL)
% Nota: se il confronto è "se possibile", puoi strutturarlo come sezione facoltativa/di prospettiva, da sviluppare solo se arrivi a implementare un prototipo Flink

% \section{Confronto Spring vs Flink (tabella riassuntiva)}
% Un buon modo per chiudere il capitolo è una tabella con criteri come:
% Modello di elaborazione (batch/microservizio vs stream nativo)
% Gestione dello stato
% Curva di apprendimento / ecosistema
% Scalabilità e fault tolerance
% Adeguatezza al caso d'uso specifico (enrichment Kafka→PostgreSQL→Kafka)

% \section{Conclusioni del capitolo}
% Riepiloga perché per l'implementazione pratica hai scelto (o proverai) queste tecnologie, aprendo il collegamento al capitolo successivo (progettazione/architettura del sistema)

\newpage
